# Taxonomy candidates (scratchpad)

Append free-form tag strings discovered during ingest or LLM compilation. The maintainer maps these to `taxonomy/canonical_tags.yml` and `synonyms.yml` during the monthly merge (see [`docs/TAXONOMY_GOVERNANCE.md`](../docs/TAXONOMY_GOVERNANCE.md)).

| Date | Candidate string | Proposed canonical or synonym | Status |
|------|------------------|--------------------------------|--------|
| | | | |
